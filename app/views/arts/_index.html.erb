<% arts.each do |art| %>
  <article>
    <% if art.liked_image.present? %>
      <span><a href="/arts/<%=art.id%>" class="image mr10"><%= image_tag art.liked_image %></a></span>
    <% else%>
      <span><a href="/arts/<%=art.id%>" class="icon fa-gem mr10"></a></span>
    <% end %>
    <div class="content container">
      <%= link_to art_path(art) do %>
        <h3><a href="/arts/<%=art.id%>"><%= art.name %></a></h3>
      <% end %>
      <p>
        <div>作者：<%= art.artist.name %></div>
        <div>制作年：<%= art.year %></div>
        <% if art.star.present? %>
          <div>★<%= art.star.round(2) %></div>
        <% end %>
      </p>
    </div>
	</article>
<% end %>